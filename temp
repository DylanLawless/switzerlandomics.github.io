Yes. “Open core” implies the same core system underlies both versions. From what you described, that is not quite right.

You have:

* a public version, which includes sources that are not for commercialisation
* a separate private in-house version, with proprietary development, more features, and support

So I would present it as two distinct offerings in one row, not as “open core”.

A better line is:

`PanelAppRex AI	Public version and private institutional version	Mixed	Free public version, commercial private deployment`

Or slightly more explicit:

`PanelAppRex AI	Public search version based on permitted public sources, plus a private institutional version with proprietary features and support	Mixed	Free public version, commercial private deployment`

For the internal table, I would mirror that:

`PanelAppRex AI	Public version based on non-commercial source constraints, plus a private institutional version with proprietary features and support	Free	CHF ...	CHF ...	CHF ...`

So the public page row becomes:


That is much clearer and avoids the wrong licensing impression.

If you want, I can rewrite the exact row for both `pricing.md` and `pricing_internal.md`.

